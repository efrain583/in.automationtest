DATASOURCE = MYSQL
DBCONN_URL = jdbc:mysql://localhost:3306/automation
DBUSER = root
DBPASS = root
#IN.AUTOMATIONTEST_RECORDVERIFICATION_SQL =  select email from recordVerification;
#IN.AUTOMATIONTEST_REGISTRATION_SQL =  select UPPER(r.fName), UPPER(r.lName), r.address, r.email as USERID, r.phone, r.sLanguage, r.skill, r.country, r.country2, r.bYear, r.bMonth, r.bDay, r.pass from registration r;